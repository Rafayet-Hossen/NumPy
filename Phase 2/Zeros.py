import numpy as np
arr_zeros = np.zeros((2, 2),int) # 2x2 array of all entry is zero
print(f'Array is {arr_zeros.shape}') #row and col in 2d array
print(f'Array is: {arr_zeros.ndim} D') #dimention of the array
print(f'Array size: {arr_zeros.size}') #size of the array
print(f'Array dtype: {arr_zeros.dtype}') #tupe of the array
print(f'Array element size: {arr_zeros.itemsize}') # size of each item
